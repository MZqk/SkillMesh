{
  "name": "skillmesh",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "description": "MCP App and companion Agent Skill for local Skill mapping, evidence review, controlled installation, and in-context handoff.",
  "scripts": {
    "mcp": "node mcp-server.mjs",
    "build:app": "node scripts/build-app.mjs",
    "check:app": "node scripts/build-app.mjs --check",
    "check:surfaces": "node scripts/check-delivery-surfaces.mjs",
    "build:plugin": "node scripts/build-plugin-runtime.mjs",
    "check:plugin": "node scripts/build-plugin-runtime.mjs --check",
    "validate:plugin": "node scripts/validate-plugin.mjs",
    "setup:pdf": "node scripts/setup-pdf.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "1.7.5",
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
